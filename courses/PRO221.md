# PRO221 — Server Admin I Project: Windows Server (Canvas ID 3631288)

## Status
First full course-wide review pass completed 2026-07-21 (pages restyled,
all 11 fittable assignments rebuilt to the 3-box template). PRO221's
lecture-page content is still much thinner than BIT221/BIT351 (only 3
topics have pages, covering Week 1 only) - see Notes.

## Notes
- **Course-wide review completed 2026-07-21**, applying the same process
  used for BIT351: orientation via `get_course_structure`, `canvas-html-lint`
  scan, `visual-template-audit`, `legacy-page-merge` check, full
  `assignment-template-rebuild` pass, and the lecture-page depth standard
  where applicable. Sequenced per [[course-review-sequencing]]: this was
  PRO221's first pass, after BIT221/BIT351/BIT281 finalized the standards.

### Pages (3 total, all under Week 1's Resources subheader)
- PRO221 has only 3 lecture/resource pages in the entire course - Performance
  Monitoring, File Services, Group Policy - all wired under **Week 1** even
  though File Services doesn't map to any Week 1 assignment (looks like
  orphaned/leftover content from an earlier course structure; not
  reorganized this pass, flagged for a future decision). **Weeks 2-5 have no
  resource pages at all** - a much bigger content gap than BIT221 or BIT351
  ever had (those were missing at most one or two weeks).
- All 3 existing pages were on an older visual style (no `max-width: 860px`
  wrapper, no header card/breadcrumb, no `hr` dividers, learning objectives
  as flex-wrap tiles instead of the strict 2-col grid) - restyled in place
  to the full BIT221 spec and given the depth-standard sections (Dependencies
  and Interactions, Security Considerations), matching the BIT221 VM Backup
  SOP precedent. Citations verified via WebSearch: cryptojacking CPU/network
  signatures, MITRE ATT&CK T1039 (Data from Network Shared Drive) and
  T1484.001 (Group Policy Modification), the MS14-025 GPP cpassword
  vulnerability (reused from BIT221), and FSRM's NTFS-only requirement.
- **Building the missing Weeks 2-5 pages was deliberately not started** -
  this is new-page-structure work requiring its own confirmation, separate
  from the template/depth-standard fixes done this pass.

### Assignments (15 lab assignments total)
- **11 assignments rebuilt** to the full Purpose/Task/Criteria template from
  having no box structure at all (a heavier lift than BIT221/BIT281's
  Task-box-only fixes, since these needed genuine Criteria content
  synthesized from each assignment's Deliverables and real CLO/PO language
  sourced from the course Home page's Learning Outcomes list and 1A's
  already-published Program Outcomes): 2A/2B/2C (Infrastructure as Code -
  IIS/DNS/DSC Pull Server), 3A/3B/3C (Docker Containers/Research Paper/NLB),
  4A (Failover Clustering), 5A-5D (Azure VM/Containerized Web App/Container
  Instances/Virtual Network).
  - 2C is unpublished and carries a red "***** NOT WORKING *****" banner -
    left exactly as-is (banner and unpublished state both preserved); only
    the template boxes were added around it.
  - **5A-5D (the four Azure walkthroughs) have no explicit Deliverables
    section at all** - they're Microsoft Learn walkthrough conversions
    ending in a "Congratulations" note and cost-cleanup reminder, with
    nothing telling students what to actually submit for grading. Criteria
    boxes were synthesized from each walkthrough's own natural checkpoints,
    but this is a real, separate content gap worth a decision: should these
    have a screenshot-document deliverable like 4A, and if so what should
    it capture?
  - Three literal em dashes found and fixed: one each in 2A, 2B (both in the
    opening explanation of what the assignment does), and one in 2C (Step
    4's "you should see the pulled website" sentence). All three fixed via
    space-hyphen-space.
- **Capstone Presentation Summaries - Extra Credit was deliberately
  skipped.** It's a peer-observation reflection assignment, not a technical
  skill demonstration - none of the course's 8 published Learning Outcomes
  or 3 verified Program Outcomes map to it without fabricating language, so
  it was left on its original format rather than forcing an ill-fitting
  Purpose/Task/Criteria structure onto it (same reasoning as skipping
  BIT221's 5C and BIT281's quiz/LTI-backed items).
- **1A, 1B, 4B were already on the current template** before this pass
  (confirmed via direct inspection - a stale claim in the old
  `assignment-template-rebuild` skill file that PRO221 was "used previously"
  for this template was verified false and corrected in that skill).

### Still open, needs Corey's decision
1. Should the Weeks 2-5 resource pages be built, and if so in what order?
2. Should File Services be moved out of Week 1 to wherever it actually
   belongs (unclear from current assignment mapping)?
3. Should 5A-5D get an explicit Deliverables section?
4. Should Capstone Presentation Summaries get any template treatment, or
   stay as-is permanently?
