# BIT221 — Server Administration I: Windows Server 2025 (Canvas ID 3631198)

## Status
Reference standard, largely built. This course defines the visual/structural
template (see CLAUDE.md "Visual template") that all other courses in scope
follow. When a standard is ambiguous elsewhere, defer to how BIT221 does it.

## Notes
- Reference pages: `bit221-course-outline` (Course Outline standard).
- Treat as the source of truth for template decisions; changes here ripple to
  BIT281, PRO221, and BIT351.
- Even the reference course had drift: `vm-backup-and-recovery-sop` (Week 1
  Resources) was found on an old six-section boxed template (900px wrapper,
  no header card/breadcrumb, plain code blocks) with no current-template
  counterpart to merge into. Restyled in place via `legacy-page-merge`'s
  isolated-old-page branch on 2026-07-20 - all content preserved, structure
  rebuilt to the current template. Confirms BIT221 needs its own periodic
  `visual-template-audit` pass, not just courses copying from it.
- Week 4 had no Resources pages at all (unlike Weeks 1, 2, 3 - Week 5 had the
  same gap, see below, corrected same week). Built three new pages on
  2026-07-21, grounded in the live 4A/4B/4C assignment content:
  `week-4-|-page-1-active-directory-administration` (delegation, AD Recycle
  Bin, default + fine-grained password policies), `week-4-|-page-2-internet-
  information-services` (IIS install GUI + PowerShell, DNS CNAME aliasing),
  `week-4-|-page-3-group-policy-objects` (GPO linking/processing order,
  Security Filtering, software deployment, drive mapping). Wired into the
  Week 4 module as a `Resources` SubHeader positioned before `Training` (the
  correct order per Weeks 1-3 - note this for future module-wiring work,
  Resources precedes Training, not the reverse). All 4 items created
  **unpublished** - Corey is doing a final review pass before publishing
  himself. Reference links use verified Microsoft Learn URLs (websearched,
  not recalled from memory).
- Per Corey (2026-07-21): BIT221's pages currently conflate two audiences -
  student-facing resource pages and instructor-facing lecture notes - into a
  single combined-style page, with no separate instructor lecture notes
  existing anywhere. This needs a real revisit (likely course-wide, not
  Week-4-scoped) but was deliberately deferred rather than solved mid-build,
  to avoid introducing a third competing page style. Flag this before any
  future full-course template work.
- Per Corey (2026-07-21): the initial Week 4 pages were too assignment-task-
  focused - good as far as they went, but missing why each topic matters,
  key concepts beyond the lab steps, real-world usage, dependencies, and
  security concerns. All three Week 4 pages were rewritten same-day with a
  deeper treatment (added "Dependencies and Interactions" and "Security
  Considerations" sections, an expanded "why this matters" passage in Core
  Concept, and additional discussion questions) - citations verified via
  WebSearch (MITRE ATT&CK, NIST SP 800-63B, Microsoft Learn), nothing
  fabricated. This depth standard is now baked into `lecture-notes-playbook`
  (see that skill) as a permanent requirement for all future page-building,
  not a one-off for Week 4.
- **Weeks 1-3 retrofit: complete (2026-07-21).** All 8 existing Weeks 1-3
  pages were already substantially higher quality than Week 4/5's initial
  drafts - most already had implicit real-world framing - but none had the
  dedicated "Dependencies and Interactions" / "Security Considerations"
  sections. Added both to every page, edited in place (these were already
  published/live, unlike Week 4/5's unpublished-pending-review workflow):
  - `week-1-|-page-1-windows-server-versions-and-editions` - attack-surface
    reasoning for Server Core, EOL patching exposure, licensing compliance
  - `week-1-|-page-2-installation-and-configuration` - RDP exposure, and a
    real correction: Microsoft actually recommends *against* disabling IPv6
    (step 7 in this page's own script), contrary to common practice
  - `week-1-|-page-3-active-directory-concepts` - filled a real gap: the
    page's own in-class activity #3 asked "why does LDAP signing matter"
    without ever teaching the mechanism anywhere on the page; now explains
    Credential Guard vs. Pass-the-Hash/Pass-the-Ticket and LDAP channel
    binding vs. NTLM relay directly, with the activity now referencing it
  - `week-2-|-page-1-active-directory-domain-structure` - the DC01-only
    window before DC02 is promoted as a real single-point-of-compromise risk
  - `week-2-|-page-2-dns-domain-name-system` - DNSSEC vs. cache poisoning/
    spoofing by name, zone-transfer (AXFR) footprinting risk and the fix
  - `week-3-|-page-1-dhcp-dynamic-host-configuration-protocol` - rogue DHCP
    servers and DHCP starvation, and the real limits of AD authorization as
    a mitigation (doesn't stop non-domain-joined rogue servers)
  - `week-3-|-page-2-users-groups-and-organizational-units` - IGDLA's audit
    payoff tied explicitly to Week 4's delegation-scope-creep theme; the
    CSV bulk-import script's plaintext-password-in-file exposure
  - `week-3-|-page-3-file-servers-shared-folders-and-ntfs` - hidden shares
    are discoverable not just guessable; breaking inheritance limits normal
    access but not administrative override capability
  All citations verified via WebSearch (MITRE ATT&CK, CISA, Microsoft Learn)
  before writing - nothing fabricated. Since these pages were already live,
  the depth additions went out immediately rather than through the
  unpublished-review workflow used for Week 4/5.
- Week 5 had the same no-Resources-pages gap as Week 4. Built three new
  pages on 2026-07-21, grounded in the live 5A/5B/5C assignment content, this
  time with the full depth standard from the start (no shallow-then-deep
  pass needed): `week-5-|-page-1-distributed-file-system-and-fsrm` (DFS
  Namespace/Replication, FSRM quotas/RMS encryption/file screening, ties to
  "DFS replication is not a backup" leading into Page 2),
  `week-5-|-page-2-backup-recovery-and-performance-monitoring` (Windows
  Server Backup, System State/BMR, file recovery, Performance Monitor
  baseline, RPO/RTO framing), `week-5-|-page-3-folder-redirection` (GPO
  folder redirection, HomeFolders$ share, Access-Based Enumeration - and its
  real limitation as a non-security feature). Wired as a `Resources`
  SubHeader before `Training`, matching the established order. All 4 items
  created **unpublished**, same review-then-publish workflow as Week 4.
  Citations verified via WebSearch (MITRE ATT&CK T1490/M1053, CISA 3-2-1
  backup rule, Microsoft Learn) - nothing fabricated. Note: this completes
  Weeks 4 and 5, so the Weeks 1-3 retrofit above is now unblocked whenever
  Corey wants to start it.
- 5C ("Folder Redirection - Extra Credit") has notably thin, generic
  assignment text compared to 5A/5B (boilerplate Purpose paragraph, an
  unverified non-Microsoft blog as its only resource link, some Word-paste
  artifacts). Not touched - out of scope for page-building - but flagged as
  a plausible future `assignment-template-rebuild` candidate if Corey wants
  it cleaned up.
- **Cross-course template discrepancy found during BIT351 review
  (2026-07-21):** BIT351's assignments use a third box type - an amber/gold
  "Task" box between Purpose and the detailed steps - that BIT221's
  two-box (Purpose/Criteria) assignment template does not have. Per Corey:
  "Not sure yet - flag it, decide later." BIT221 stays on the two-box
  template until this is explicitly resolved; see `courses/BIT351.md` for
  the full note.
