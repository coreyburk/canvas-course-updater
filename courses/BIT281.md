# BIT281 — Hardware Systems (Canvas ID 3631199)

## Status
Rebuild complete.

## Notes
- **Assignment template standardization (2026-07-21):** BIT281's assignments
  had a bare, unstyled `<h3>Task</h3>` heading (no box) and a div-based, not
  table-based, Criteria for Success box - both drift from the newly-decided
  canonical 3-box template (Purpose/Task/Criteria, standardized on BIT351's
  styled implementation). Fixed via a background agent across all 9 lab
  assignments:
  - **7 updated** (Build the Best Computer, Laptop Breakdown, CPU-Z, GPU-Z,
    TRIM, Computer Parts Identification, System Inventory): Task wrapped in
    the standard yellow table box, Criteria div converted to the standard
    green table box, all existing wording preserved. Where the old Task
    section had multiple intro paragraphs, only the genuine 1-2 sentence
    task summary went inside the box - the rest (resource links,
    "Directions:" labels, instructor logistics) stayed after the box as
    detailed instructions, per judgment call logged per-assignment.
  - **2 skipped** (Memory Identification, GPU Scavenger Hunt): no HTML to
    convert - these are quiz-backed and external-tool (LTI)-backed
    assignments respectively, with an empty description field.
  - No em dashes found in any of the 7 rewritten assignments.
  - **Flagged, not fixed:** CPU-Z and System Inventory have point values
    ("(10pts)", "(1pt each)", "(5pts)", "(2pts each)") inside their
    Task/Deliverables sections - not inside the Criteria for Success box,
    so not a rule violation under the current standard, but a recurring
    pattern worth a future decision on whether points belong in assignment
    prose at all outside the rubric.
