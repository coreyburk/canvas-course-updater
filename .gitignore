# Secrets — never commit
.env
.env.*
*.token
*secret*
*credentials*

# OS / editor
Thumbs.db
Desktop.ini
.DS_Store
*.swp

# Claude Code local overrides (may contain machine-specific paths/keys)
.claude/settings.local.json

# Scratch / temp
scratchpad/
*.tmp
